import React from "react"
import style from "./UserSelect.module.sass"

const UserSelect = (props) => {
    return (
        <div className={style.userSelect}>
            {

            }
        </div>
    )
};

export default UserSelect;