import React from "react"
// import style from "./SelectedItem.module.sass"

const SelectedItem = (props) => {
    return (
        <div className={"SelectedItem"}>
            {
                "SELECTED ITEM"
            }
        </div>
    )
};

export default SelectedItem;